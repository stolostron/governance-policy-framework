apiVersion: apps/v1
kind: Deployment
metadata:
  name: governance-policy-status-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      name: governance-policy-status-sync
  template:
    metadata:
      labels:
        name: governance-policy-status-sync
        app: policy-framework
    spec:
      serviceAccountName: governance-policy-status-sync
      containers:
        - name: governance-policy-status-sync
          image: quay.io/stolostron/governance-policy-status-sync:latest-2.3
          command:
          - governance-policy-status-sync
          args:
            - '--hub-cluster-configfile=/var/run/klusterlet/kubeconfig'
            - '--enable-lease=true'
          imagePullPolicy: Always
          volumeMounts:
            - name: klusterlet-config
              mountPath: /var/run/klusterlet
          env:
            - name: WATCH_NAMESPACE
              value: managed
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "governance-policy-status-sync"
      volumes:
        - name: klusterlet-config
          secret:
            secretName: hub-kubeconfig
            defaultMode: 420
